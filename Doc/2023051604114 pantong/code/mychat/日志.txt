1.单例文件singleton
（1）全局唯一发对象，不可赋值，复制。通过statict share-ptr智能计数指针管理唯一实例

（2）通过static函数GetInstance获取实例
（3）template<typename T>calss singleton
 (4)只有。头文件
（5）在私有中构造函数

2.httpmgr
（1）传输json（键值对）-qbtye（网络传输格式）
（2）通过qt的httpmgr post发送请求
（3）std::enable_shared_from_this<>中的share——from——this返回当前对象的指针，防止销毁。
（4）crtp模式派生类继承自己的基类（静态多态）适合很多类需要单例
虚函数是（动态多态）
